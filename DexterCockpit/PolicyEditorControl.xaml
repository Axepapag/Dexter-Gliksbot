<UserControl x:Class="DexterCockpit.PolicyEditorControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF121212">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8" VerticalAlignment="Center">
            <Button Content="Reload" Width="90" Height="28" Click="OnReload" Margin="0,0,8,0"/>
            <Button Content="Save" Width="90" Height="28" Click="OnSave" Margin="0,0,8,0"/>
            <Button Content="Activate" Width="90" Height="28" Click="OnActivate" Margin="0,0,24,0"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,0">
                <TextBlock Text="Profile:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="ProfileCombo" Width="180" SelectionChanged="OnProfileSelectionChanged"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Background="#FF1E1E20" Padding="10" Margin="0,0,8,0">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="CatalogContainer">
                        <TextBlock Text="Profile Details" FontWeight="Bold"/>
                        <TextBlock x:Name="ProfileDescription" TextWrapping="Wrap" Margin="0,0,0,8"/>
                    </StackPanel>
                </ScrollViewer>
            </Border>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBox x:Name="PolicyText"
                         Grid.Row="0"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Auto"
                         FontFamily="Consolas"
                         FontSize="13"
                         TextWrapping="NoWrap"
                         IsReadOnly="True"
                         Background="#FF1F1F21"/>
                <Border Grid.Row="1" Background="#FF1F1F21" Padding="8" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock x:Name="StatusText" Foreground="#FFCCCCCC" TextWrapping="Wrap"/>
                        <TextBlock x:Name="RuleCountsText" Foreground="#FF88888D" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <TextBlock Grid.Row="2" x:Name="CatalogHint" Foreground="#FF6F6F74" Margin="0,8,0,0" Text="Toggle the checklists to adjust deny rules. Save to persist, Activate to set the live mode."/>
        <TextBlock Grid.Row="3" x:Name="HealthText" Foreground="#FF6F6F74" Margin="0,4,0,0"/>
    </Grid>
</UserControl>
