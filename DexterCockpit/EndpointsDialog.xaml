<Window x:Class="DexterCockpit.EndpointsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dexter Endpoints"
        Height="220"
        Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="HTTP Endpoint" FontWeight="Bold"/>
        <TextBox x:Name="HttpUrlBox"
                 Grid.Row="1"
                 Margin="0,6,0,10"
                 Height="28"/>
        <TextBlock Text="WebSocket Endpoint" Grid.Row="2" FontWeight="Bold"/>
        <TextBox x:Name="WsUrlBox"
                 Grid.Row="3"
                 Margin="0,6,0,16"
                 Height="28"/>
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Grid.Row="4"
                    Margin="0,8,0,0">
            <Button Content="Reset to Default"
                    Width="140"
                    Margin="0,0,12,0"
                    Click="OnResetDefaults"/>
            <Button Content="Cancel"
                    Width="80"
                    Margin="0,0,8,0"
                    Click="OnCancel"/>
            <Button Content="Save"
                    Width="80"
                    IsDefault="True"
                    Click="OnSave"/>
        </StackPanel>
    </Grid>
</Window>
