<UserControl x:Class="DexterCockpit.Views.AgentRosterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="300"
             Background="#252526">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="#2D2D30" Padding="8">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding LoadAgentsCommand}" 
                        Style="{StaticResource MaterialDesignIconButton}"
                        ToolTip="Refresh Agents"
                        Width="32" Height="32">
                    <materialDesign:PackIcon Kind="Refresh" Width="20" Height="20"/>
                </Button>
                <TextBlock Text="{Binding StatusMessage}" 
                           VerticalAlignment="Center" 
                           Margin="10,0,0,0"
                           Foreground="LightGray"
                           FontSize="11"/>
            </StackPanel>
        </Border>

        <!-- Agent List -->
        <ListBox Grid.Row="1" 
                 ItemsSource="{Binding Agents}"
                 SelectedItem="{Binding SelectedAgent}"
                 Background="Transparent"
                 BorderThickness="0"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                    <Setter Property="Padding" Value="8"/>
                    <Setter Property="Margin" Value="4"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Background="#2D2D30" 
                            BorderBrush="{Binding StatusColor}" 
                            BorderThickness="2" 
                            CornerRadius="4"
                            Padding="10">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- Agent Name and Status -->
                            <Grid Grid.Row="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <materialDesign:PackIcon Grid.Column="0" 
                                                         Kind="{Binding StatusIcon}" 
                                                         Width="24" Height="24"
                                                         Foreground="{Binding StatusColor}"
                                                         VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="1" 
                                           Text="{Binding Name}" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="10,0,0,0"/>

                                <Ellipse Grid.Column="2" 
                                         Width="10" Height="10" 
                                         Fill="{Binding StatusColor}"/>
                            </Grid>

                            <!-- Metrics -->
                            <Grid Grid.Row="1" Margin="0,8,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="Uptime" FontSize="10" Foreground="Gray"/>
                                    <TextBlock Text="{Binding UptimeFormatted}" FontSize="11" Foreground="White"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="Success Rate" FontSize="10" Foreground="Gray"/>
                                    <TextBlock Text="{Binding SuccessRateFormatted}" FontSize="11" Foreground="LimeGreen"/>
                                </StackPanel>
                            </Grid>

                            <!-- Current Mission -->
                            <TextBlock Grid.Row="2" 
                                       Text="{Binding CurrentMission}" 
                                       FontSize="10" 
                                       Foreground="Cyan"
                                       Margin="0,4,0,0"
                                       TextTrimming="CharacterEllipsis"/>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Quick Actions -->
        <Border Grid.Row="2" Background="#2D2D30" Padding="8" BorderBrush="#007ACC" BorderThickness="0,1,0,0">
            <StackPanel>
                <TextBlock Text="Quick Actions" FontSize="11" Foreground="Gray" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding PauseAgentCommand}" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#FFB900"
                            ToolTip="Pause Selected Agent"
                            Margin="4"
                            Width="60" Height="32">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Pause" Width="16" Height="16"/>
                            <TextBlock Text="Pause" Margin="4,0,0,0" FontSize="10"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding ResumeAgentCommand}" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#00CC6A"
                            ToolTip="Resume Selected Agent"
                            Margin="4"
                            Width="60" Height="32">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Play" Width="16" Height="16"/>
                            <TextBlock Text="Resume" Margin="4,0,0,0" FontSize="10"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding StopAgentCommand}" 
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Background="#E81123"
                            ToolTip="Stop Selected Agent"
                            Margin="4"
                            Width="60" Height="32">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Stop" Width="16" Height="16"/>
                            <TextBlock Text="Stop" Margin="4,0,0,0" FontSize="10"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
